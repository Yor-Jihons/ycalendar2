import * as CheckedDates from "../src/checkeddates";


describe('Test for ycalendar.ts', () => {
    test( 'Sample of the class ycalendar', () => {
        //expect( CheckedDates.CheckedDateType.Unchecked ).toBe( 0 );
    });
});
